# 生成物・依存・一時領域（先頭 / はリポジトリ直下に固定。素の名前は全階層に効くため使わない）
node_modules
bun.lock
subagents
/dist
/lib
/**/dist
test/fixtures
# 意図的に壊れた HTML フィクスチャ等（パーサーが読めない検証用データ）
sample
**/*.html

# dev用外部取得物（TM本体・Chrome for Testing。dev-setup.ts が取得・検証する）
/dev-extensions
/dev-assets
