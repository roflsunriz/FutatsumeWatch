# 生成物・依存・一時領域
node_modules
dist
**/dist
lib
**/lib
bun.lock
subagents

# 段階移行の例外: 既存 .js 資産はファイル単位の TS 変換時に整形する。
# TS へ変換したファイルはこの例外から外し、prettier 準拠を必須とする。
# 新規ファイル（.ts/.mjs/.json/.md）は最初から prettier 準拠とする。
src/**/*.js
packages
test/**/*.js
test/browser
*.js
!eslint.config.mjs

# 意図的に壊れた HTML フィクスチャ等（パーサーが読めない検証用データ）
sample
**/*.html

# 実 API スナップショット等の JSON フィクスチャ（空白正規化は差分 noise のみ）
test/fixtures

# 旧ビルド資産（babel/eslint4 系設定。Bun 移行時に削除予定）
.babelrc
.eslintrc
packages/components/.babelrc
