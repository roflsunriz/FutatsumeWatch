# 変更履歴

Keep a Changelog形式。日付はYYYY-MM-DD。

## [Unreleased]

## [0.0.24] - 2026-09-25

### Fixed

- プレイヤーを閉じて開き直しても保存したロック設定が戻らない問題を修正し、開くたびに詳細パネルを開いた状態で復元するようにした。

## [0.0.23] - 2026-09-25

### Added

- 右側サイドバーのロック設定を保存し、次回起動時に詳細パネルを開いた状態で復元するようにした。

### Fixed

- 右側サイドバーをロックしたまま設定を開いてもロックが解除されないよう、設定操作でロック状態・詳細パネル・配置を変更しないようにした。ロックの解除はロックボタン操作だけが行う。

## [0.0.22] - 2026-09-25

### Added

- 共通指針の求める報告・議論の入口が欠けていたため、PRテンプレート、Issueテンプレート（bug-report・config・documentation）、Discussionテンプレート（general・ideas・q-a）を追加した。秘密情報を貼らずに版・環境・再現手順を集められるようにした。
- 依存更新と変更範囲の把握が欠けていたため、Dependabot設定とlabeler設定、PRへ自動でラベルを付けるワークフローを追加した。リリースワークフローはCHANGELOGの対象版を自動抽出する現行の検査を維持する。

### Fixed

- CI と Dependabot の分類の実行順が前後しても更新を取りこぼさないよう、同じ PR 番号と head SHA を再照合する経路を追加した。
- actions/labeler v7でPRの自動ラベル付けが設定形式エラーにならないよう、ラベル条件を`changed-files`形式へ更新した。

### Changed

- 依存更新を安全に省力化するため、Dependabot の patch／minor PR を既存 CI の全チェック成功後に自動取り込みし、失敗ジョブを一度再実行する設定を追加した。
- MylistPocketの動画詳細のタグ表示を詳細タブ・再生中メタデータと同じ描画へ共通化し、大百科アイコンの存在解決を公式記事API照会へ一本化した。未取得・あり・なしの表示は共通ヘルパーで統一する。

### Removed

- 本体のNGタグ・NG投稿者を削除するため、`videoTagFilter`／`videoOwnerFilter`設定・設定画面の入力欄・`VideoFilter`と再生除外・次動画送り・検証台帳・関連テストを削除した。設定は34項目になる。保存済みの旧キーは読み込み対象外として保持する。MylistPocket側のNG・お気に入り判定は維持する。

## [0.0.21] - 2026-09-22

### Changed

- 一覧の起動ボタンをタイトル行の後ろから公式サムネ内の右上へ重ねる配置に変更し、ホバー待ちの表示切り替えをやめて常時表示にした。ニコ百埋め込みボタンも常時表示にし、行の崩れや再生時間・ランク表示との重なりを避ける。
- 再生中にホバーで表示されるタイトルと動画メタデータの下へ、タグ一覧を表示するようにした。詳細タブと同じ閲覧専用の表示を使い、編集機能は追加しない。

### Fixed

- 実利用環境のnicocache_nl／filter-matome由来の付加クラス・付加要素・画質文言へ起動ボタンの設置・説明文が結合しないよう、公式のサムネ構造だけを正本にして統合するようにした。Nアニメのカード全体リンク・大百科の広告スライドと本文リンクにも対応し、説明文の先頭duration・末尾再生数・本文アイコンaltを除去する。
- 金属調トグルへ移行した後に残っていた旧来の2倍拡大指定を除去し、NG・フィルターと詳細設定のトグルスイッチが互いに重ならないようにした。

## [0.0.19] - 2026-09-22

### Changed

- コメントのフォント設定を自由入力からプルダウンへ変更し、Canvasの実描画幅で利用可能と判定できたローカルフォントと汎用ファミリーだけを選べるようにした。既存の保存値は保持し、利用不能な値は選び直せるよう無効表示する。
- 手描きUI案に合わせ、設定ボタンから5カテゴリを平置きした左サイドバーと選択中カテゴリの内容を表示する右パネルを直接開くようにした。左側へ画質・GitHub・再読み込み・画像保存・公式視聴ページも常設し、チェック設定はOFFが灰色、ONが緑色の金属調トグルへ統一した。
- UI案2に合わせ、動画詳細の4タブに共通の固定ボタンを追加した。固定中は背景ぼかしを外して操作UIを有効に保ち、映像・コメント面・操作バーを右パネルと重ならない左側の領域へ収める。

### Fixed

- 動画詳細を固定した際にコメントCanvasだけが元の全画面寸法に残る問題を修正し、右パネルと重ならない表示領域へ外枠・Canvas・内部画素を同時にリサイズするようにした。
- 大百科記事の有無を動画APIの古いフラグで確定しないよう、全タグを公式記事APIへ照会し、2xxを「あり」、404を「なし」、その他の失敗を「不明」としてアイコンへ反映するようにした。

### Removed

- 有効性検証が必要な書き込み導線を減らすため、タグの編集・追加・削除・再取得ボタン、タグ編集API実装と専用テストを削除した。タグ表示・検索・大百科リンクは閲覧専用で維持する。
- 検証対象を減らすため、関連動画・プレイリスト行の「マイリスト追加」「とりあえずマイリスト追加」と、動画詳細情報の「とり」「マイ」「公開」「Twitter」「設定」ボタンおよび専用の選択画面を削除した。
- 検証できない設定と保守負担を減らすため、複数タブ起動、公式プレイヤー／動画リンク置換、マイリスト投稿者説明、動画ID指定の＠ジャンプ、タッチジェスチャ、FutatsumeTube自動画質、期限切れアニメの別映像取得を設定・実装・テストから削除した。
- 詳細設定を「画面ダブルクリックでフルスクリーン切り換え」「再生終了時に自動でフルスクリーン解除」の2項目へ整理し、ページ継続、背景ダブルクリック終了、自動FutatsumeTube、タッチ割当、デバッグモードを削除した。
- HLSの可変設定画面と保存値、MaskedWatch、GamePad、HeatSyncを製品・設定導線・テストから削除した。Domand動画の再生に必要なHLS処理は固定設定で維持する。

## [0.0.18] - 2026-09-22

### Changed

- コメントサーバーで実用できる日本語だけを扱うため、コメント言語の既定動作を日本語へ固定した。動画APIが発行した`nvComment.params`は通信契約として引き続き改変しない。

### Removed

- 利用できない英語・中国語へ切り替わる操作をなくすため、コメント言語設定と移行・同期処理、コメント一覧メニューの日本語／英語／中国語切り替えを削除した。メニューは「コメントを位置順に並べる」「新しい順」「ニコる数」の3項目だけを表示する。

## [0.0.17] - 2026-09-21

### Fixed

- NG正規表現の編集場所を要件どおり一つにするため、詳細設定の欄を削除し、一般設定の「NG・フィルター」にあった「NGワード」欄を、1行ごとの`/パターン/フラグ`を入力する複数行欄へ置き換えた。
- コメント行の「NGword」で選んだ本文を安全なリテラル正規表現へ変換し、既存のNG正規表現を保持したまま1行追加するようにした。旧NGワードと旧単一正規表現／フラグも同じ一覧へ自動移行する。

## [0.0.16] - 2026-09-21

### Fixed

- 保存されたコメント言語が動画APIの`nvComment.params.language`と異なるとコメントAPIが`INVALID_PARAMETER`を返すため、サーバー提供のparamsを送信前に書き換えず、表示側の言語状態も実際のAPI言語へ同期するようにした。

## [0.0.15] - 2026-09-21

### Changed

- 会員状態にかかわらずシークバーのサムネイル表示を設定できるよう、DomandのStoryboard取得可否を設定と実API応答で判定するようにした。
- 初期状態で意図せず操作や速度変更を有効にしないよう、画面クリック再生・GamePad・HeatSyncの既定値をOFFへ変更した。
- NG正規表現とフラグを`/パターン/フラグ`形式の1入力へ統合した。NGタグとNG投稿者は一般設定の「NG・フィルター」へ移動した。
- 配布物とコンソールを簡潔に保つため、通常ログ・計測ログとuQueryのコンソール用探索・ダンプ・プロファイル機能を削除した。

### Fixed

- Nアニメとニコ百で、見えている起動ボタンをカードや埋め込みプレイヤーの透明リンクが覆っていても、ボタン位置の実クリック・タップからFutatsumeWatchを起動できるようにした。
- モバイルの埋め込みサムネイルでホバー操作を要求しないよう、起動ボタンを常時表示し44px以上の操作領域にした。
- Storyboard取得失敗時に失敗表示のclass更新を連結して例外になる問題を修正した。

### Removed

- 現行UIと描画経路を簡潔にするため、「ニコニ広告の情報を取得する」「ボタンの大きさ」「倍速再生でもコメントは速くしない」「コメントを動画の後ろに流す」「コメントの速度」と、コメント影の「縁取り2」「ドカベン」を設定・実装・テストから削除した。
- ニコニ広告の提供者取得・表示と、参照されていなかった旧一般設定パネルを削除した。

## [0.0.14] - 2026-09-21

### Fixed

- シリーズ全体のプレイリスト追加を現行の`/v2/series/{id}`へ対応し、`items[].video`を共通の動画項目へ変換して全件を順序どおり追加するようにした。再生中の動画も追加後の選択位置として保持する。

## [0.0.13] - 2026-09-21

### Changed

- 外部ホストでもコメントを取得できるよう、nv-comment通信をiframeブリッジから信頼済みHTTPSホストへの直接XMLHttpRequestへ変更した。通常取得はfilter-matomeと同じ`params`・`threadKey`、JSON Content-Type、frontend／client OSヘッダーを使い、過去ログ時だけ公式通信の`additionals.when`を追加する。

### Fixed

- コメント取得失敗後に3秒待って別threadKeyで自動再送していた処理を廃止し、1回の要求結果をそのまま表示へ返すようにした。
- `globalComments`がない有効なnv-comment応答も、全threadの件数から合計を算出して処理できるようにした。

## [0.0.12] - 2026-09-21

### Added

- Nアニメ等のニコニコ公式サイトからその場で再生できるよう、表示中の動画リンクへ共通の起動アイコンを追加した。外部ホストからも通常の映像・コメント取得経路へ接続する。

### Fixed

- 同じ動画IDへのサムネイル・タイトル・詳細リンクが複数ある場合に、起動アイコンを複数作らず、表示中の代表リンクへ1個だけ配置するようにした。
- ニコ百の記事内にある埋め込みサムネイルの起動要求が親ページへ届くよう、`postMessage`の送信先を文書URLではなく検証済みのoriginへ正規化した。クエリ付き埋め込みURLからも動画IDだけを取得する。
- 単発実測が成功した後の終了処理で、遮断中の要求とCDP切断が競合して結果だけ失敗扱いになる問題を修正した。遮断はコンテキスト破棄まで維持する。

## [0.0.11] - 2026-09-21

### Added

- 承認した操作だけを1回実行できるよう、投稿・タグ・検証用マイリストの内容照合と使い切り許可を追加した。失敗時も同じ許可で再送せず、作成・追加した対象だけを後片付けできるようにした。

- 実サイトでの失敗を追加通信なしで調査できるよう、1操作限定の通信採取・重複要求遮断・認証情報マスク・試行IDの再実行防止を追加した。公式側と本体Workerの再生権取得を分離し、HTTPエラー本文と記録不能理由も残す。

- 実操作に基づく品質確認を継続できるよう、自動テスト計画と機能別台帳に合格条件、異常系・競合操作、検証層、対応テストと証跡の確認方法を整理した。
- 外部へ投稿せず機能の接続を検証できるよう、生成した2画質のHLS、状態を保持するコメント・タグ・マイリストAPI、ページとWorkerの通信遮断を使うオフラインブラウザ検証を追加した。実行ごとの証跡とCIのブラウザ検査を接続する。
- 関連動画・プレイリストの各行から追加先を選べるよう、通常マイリストの選択画面を追加した。
- 再生中に手元で投稿できるよう、音量バー横へコメント投稿フォームを配置した。色・サイズ・位置のパレット、75文字カウンター、Enter送信・Shift+Enter改行、送信中の重複防止、失敗時の本文保持と再送に対応する。未ログイン・読み込み中・過去ログ等では投稿できない理由を表示する。

### Changed

- 手動検証中の操作を妨げないよう、`bun run dev`をheadedブラウザとTampermonkey・ユーザースクリプトの導入までに限定した。自動テストは`bun run test:browser`へ分離し、別のheadlessブラウザで実行する。既存タブの自動再読み込みも廃止した。

- ファイルを探しやすく命名の揺れを防ぐため、既存のソース・テストと関連フィクスチャのファイル名をケバブケースへ統一し、importや文書の参照とlintの命名チェックを更新した。

- 画面表示と外部連携を同じ名称で扱えるよう、旧製品名のAPI・イベント・DOM・追加機能・内部識別子をFutatsumeWatch系へ統一した。旧名の外部連携は更新が必要になる。
- 更新で設定を失わないよう、HLS・GamePad・プレイリスト・MylistPocket同期とTube設定名を自動移行し、旧設定ファイルの読み込みにも対応した。新名称側の値を優先し、旧データは復旧用に残す。

### Fixed

- 短命なWorkerが監視開始中に終了しただけでブラウザ検証全体が失敗し、タグのリリース作成が止まる問題を修正した。対象セッションの消滅以外の通信・製品エラーは引き続き失敗として扱う。
- 英語ロケールのCIでも過去ログ日時を実入力できるよう、日時欄の月・日・年の順序と12/24時間制をブラウザのロケールから決定するようにした。
- ブラウザ検証の終了時にページとともに無効になった通信要求への応答失敗で、成功済みの検証を失敗扱いする競合を修正した。終了中の要求消滅以外のFetchエラーは引き続き失敗にする。
- ページのログイン表示がまだ読み込まれていない場合でも投稿欄を正しく利用できるよう、動画情報から取得した閲覧者の状態を反映する。動画切替後の古い応答では状態を変えない。
- 通信採取中の広告等の失敗で再生全体を止める問題、関連読取の事前通信を誤って遮断する問題、採取再開時の本文ファイル衝突を修正した。認証本文のbase64副本も保存時にマスクする。

- 短い間隔で設定を変更したとき、古い通知で画面や再生速度が前の値へ戻らないよう、保存モデルとプレイヤー状態の通知順序・再入更新を修正した。
- 画質の選択と映像が食い違わないよう、利用可能な画質だけを表示し、連続した画質変更を連打防止で捨てないようにした。再読み込み時の時刻と停止状態、終了・切替後の遅延応答も扱う。
- 停止中の再読み込みを中断して次の動画へ移ったとき、旧動画の一時停止指定が次動画へ残らないようにした。
- 自動再生設定を実際の再生要素へ接続し、画質変更時の一時的な再生状態が次の動画へ残らないようにした。
- 設定値を失わないよう、空・範囲外の入力、ファイル選択取消、不正な設定ファイル、容量不足を検証し、保存失敗時は旧値を保持する。設定読み込みの途中失敗では適用済み保存値を復元する。
- NG設定が遅延通知で古い値へ戻る競合と、正規表現のg/yフラグにより同じコメントの判定が交互に変わる問題を修正した。
- 投稿の重複や別動画への誤反映を防ぐため、送信中のフォーム・プレビュー・通信の寿命をそろえ、拒否時の本文保持、受理不明時の無条件再送防止、期限切れキーの再取得を整備した。
- タグ編集で失敗時の入力を保持し、権限・ロック・古い応答を判定する。大百科の有無とリンク、コメント行メニューのクリック領域も修正した。
- 記事があるタグを「なし」と表示しないよう、大百科の公式記事照会を追加した。コメント削除とニコるは受理後にだけ画面へ反映し、拒否を成功扱いしない。
- プレイリストの重複追加、逆順後の選択位置、破損保存データと保存復元、マイリストの2ページ目以降が欠落する問題を修正した。
- GamePadの軸保持・長押し・機器切替・切断復帰、HeatSyncを無効にした後の速度復帰、HLS設定のfalse値のリセットを修正した。
- HeatSyncの除外語の大文字・小文字をそろえて照合し、短い動画や除外タグの動画へ切り替えたときに前の加速が残らないようにした。利用者が手動で選んだ速度は保持する。
- Storyboardと提供者表示で、終了・設定OFF後の遅い応答が表示を復活させないようにした。マイリスト選択画面にも、対象動画の投稿者を説明へ付ける設定を反映した。
- 動画情報パネルの再生準備イベントを正しい名前へ接続し、説明欄のYouTube切替設定と関連取得を動作させた。自動切替は設定OFF・別動画・終了後には実行しない。
- 配信方式の画質比較から利用不可の候補を除き、再生できない高画質に引かれて低画質の方式を選ばないようにした。
- 顔・文字検出APIがない環境でMaskedWatchが初期化エラーにならず、設定画面に利用できない理由を表示するようにした。
- 不正なコメント時刻や未確定の動画時間でヒートマップの密度が壊れないよう、範囲外データを除外した。
- 未使用の宣言・引数と不要なlint抑制を整理し、新しい警告を見逃さないよう警告0件を品質確認の条件にした。
- HLS再生の致命的エラーを判別できるよう、計算した通信・デコードのエラー種別を通知へ反映した。

### Removed

- 現行のDomand HLS配信だけを明確に扱えるよう、旧配信方式の自動選択・HLS限定設定・画質メニュー・セッション／ストーリーボード処理と、HeatSyncの配信方式別設定を削除した。
- 元計画の指定に従い、ニコニコ市場の入口・表示枠・取得処理を撤去し、使えない操作と空白を残さないようにした。

## [0.0.9] - 2026-09-20

### Added

- 初期化から実再生までの退行を検出するため、動画ページの起動ボタン、Tampermonkey経由の操作検証、別ページ機能の通信遮断テストを追加した。
- 設定を失わず更新できるように、旧ZenzaWatch設定からの移行と復旧手順を追加した。
- コード変更と配布物の乖離を防ぐため、CIの品質確認・生成物一致検査を追加した。
- 更新・貢献・サポート・セキュリティ・行動規範の文書を整備した。

### Changed

- 配布物を小さく保ちつつ再生を壊さないよう、圧縮を空白・コメントの削減に限定した。Workerへ渡す関数の名前変更・構造変換は行わない。
- 旧ブラウザテストのイベント検証をBunテストへ移し、通常のCIでも実行するようにした。
- 配布を単一ユーザースクリプトへ統一するため、古いnpm梱包設定を除去してnpmへの公開を無効にした。

- 設定画面への入口を重複させないよう、設定を開く前の左メニューを「設定」「画質」「GitHub」「その他操作」の4項目に整理した（0.0.8）。個別設定は共通設定画面のタブから開く。

- 設定項目を折りたたみから探す手間と画面サイズの変化をなくすため、固定サイズの設定画面と左サイドバーの9カテゴリへ変更した（0.0.7）。一般設定のタブ切替時は入力内容を保持し、ほかの設定にも直接移動できる。HLSの説明も開閉せず読めるようにした。

- 設定ごとに異なっていた見た目と閉じ方を統一するため、一般設定・詳細設定・HLS・MaskedWatch・GamePad・HeatSyncを共通の設定パネルへ移行した（0.0.6）。背景ぼかし・背景クリック・Escape・右上の閉じるボタンに対応し、狭幅と全画面でも操作できるようにした。
- HLS設定のスライダーを狭幅でも読める配置にし、数値を標準の出力欄に表示するようにした。

- スケッチに合わせて動画をブラウザ内いっぱいに表示し、中央の再生操作、下部の再生設定とヒートマップ付きシークバー、左の設定メニュー、右の4タブへ再配置した（0.0.5）。操作UIは未操作3秒で隠し、パネル表示中は背景をぼかしてクリックで閉じる。
- 区間を繰り返し視聴できるABリピートを追加した。開始A・終了B・解除の順に操作し、動画切替と終了時に解除する。
- 関連動画・コメント・プレイリストの機能を引き継いで配色と余白を整え、取得できるいいね数をメタデータと動画一覧に表示した。

- コメントの文字計測・配置・描画をnpmのcomment-overlay 4.1.6へ移行し、NG・設定・動画との同期・コメント保存を新エンジンへ接続した（0.0.4）。コメントHTMLはエンジンを内包し、通信なしで再生・停止・シークできる。
- 動画終端のコメント時刻を旧実装でランダム補正せず、新エンジンで処理するよう統一した。配置と影の外観は旧版から変わる。
- 視聴ページの起動ボタンをタイトルと投稿者プロフィールの間へ移し、検索結果とともに四角形を二つ重ねたアイコンだけに統一した。右下の起動ポップアップは削除した（0.0.3）。

- 製品、テスト、ビルド補助をTypeScriptへ移行し、strict・any禁止・allowJs:false・allowUmdGlobalAccess:falseで検査する構成にした。
- Bunから実行するVite＋vite-plugin-monkeyへ移行し、旧連結器を介さずimport依存から配布物を生成するようにした。
- 個別スクリプトを何本も導入する必要がないよう、HLS、GamePad、詳細設定、MylistPocket、MaskedWatch、HeatSync、CapTube、ブログパーツ、MylistFilter、uQueryをFutatsumeWatch.user.jsに同梱した。distはこの1件のみとする。
- 起動に外部スクリプト取得を必要としないよう、lodash・jQuery・lit・hls.jsと必要なモジュール実行支援を同梱した。ホストページのライブラリは上書きしない。
- 製品名と配布先をFutatsumeWatchへ統一し、旧window名・イベント・保存値との連携互換を維持した。

### Fixed

- 同じイベント処理を複数の名前空間へ登録したとき、一つを解除すると他の登録まで失われる問題を修正した。
- 既定の圧縮でWorker内の変数参照が壊れ、動画再生が停止する問題を修正した。

- 一般設定が非同期の内容を描画せず`[object Promise]`だけを表示する問題と、詳細設定がプレイヤーの背後へ隠れる問題を修正した。一般設定には閉じるボタンとEscape操作を追加した。

- 空のコメント一覧を参照した後に投稿プレビューが二重登録され、取り消しても残る問題を修正した。
- 移行時に動画とコメントの重なり・縦横比・透明度を二重適用しないよう調整し、全画面・狭幅・停止中の表示を確認した。
- 検索結果に明示的な再生ボタンを追加し、検索から視聴ページへ再読み込みなしで移っても起動パネルが表示されるようにした（0.0.2）。
- 起動中・完了・失敗の状態を画面に表示し、以前の使い方を知らなくても起動できるようにした。
- 開発用マネージャへの登録だけで成功とせず、ページへの適用を確認して既存タブも更新するようにした。
- ブラウザで戻った検索ページを再生画面が覆う問題と、履歴のstateを壊す処理を修正した。

- 旧連結スコープのconsole再代入、uQuery空生成、未接続の設定・イベント依存による起動停止を修正した。
- TypeScriptのクラスフィールド生成で親クラスのDOM初期化を壊す問題を修正した。
- 現行動画情報APIのdmcInfo:nullで再生が停止する問題を修正した。
- バンドル後のWorkerクラスの匿名化で構文エラーになる問題と、HLS先読みの未定義参照を修正した。
- 実行していない状態を成功にしないよう、ユーザースクリプトの有効化判定と動作確認の合格条件を修正した。
- 詳細設定をクリックとキーボードで閉じられるようにした。
- 狭い画面で動画が左右に切れ、操作バーが重なる問題を修正した。
- CapTubeの作者DOM欠落、文字入力中のキー横取り、埋め込みサムネイルの非同期返答を修正した。
- プレイリスト保存失敗時に無関係なsessionStorageを削除する処理を除去した。

### Removed

- 現行の開発経路と混同しないよう、実行不能なmake.bat、未使用のloader.html、対象履歴のない.mailmap、JSHint設定、空のテスト・雛形、未参照の互換用ファイルと部品、旧デモとモックを除去した。旧ブラウザテストの検証内容はBunへ移し、コメントアートの比較資料は保持した。

- 旧CSSコメント描画・文字計測iframe・コメント配置Workerと、旧Flashスロットの設定を削除した。保存済みの設定データ自体は削除しない。
- 使用しなくなった連結ビルダー・Babel・webpack・gulp・mocha関連の設定と依存、重複配布物を除去した。
- 外部APIやソースが存在せず実行不能だった旧テストを整理し、現行データを使うBunテストへ移行した。

### Security

- 依存監査で検出した旧jsdomの推移依存の脆弱性を解消するため更新した。

## 検証範囲

本体の起動・再生・コメント・主な操作は実測済み。全機能の網羅的な動作確認は未完了で、認証・実ゲームパッド・検出API・現行マイリスト画面等の制約はverification.mdに記録する。
